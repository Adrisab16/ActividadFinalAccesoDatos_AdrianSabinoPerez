package com.example.actividadfinalaccesodatos_adriansabinoperez.JavaFX

/*
//import de.felixroske.jfxsupport.FXMLController
import javafx.event.ActionEvent
import javafx.fxml.FXML
import javafx.scene.control.Alert
import javafx.scene.control.Button
import javafx.scene.layout.VBox
import org.springframework.stereotype.Component

@Component
class SubmenuController {

    @FXML
    private lateinit var submenu: VBox

    private lateinit var operacion: String

    fun inicializar(operacion: String) {
        this.operacion = operacion
    }

    @FXML
    fun realizarOperacion(event: ActionEvent) {
        val button = event.source as Button
        val opcion = button.text

        val mensaje = "Realizando operación: $operacion en $opcion"
        mostrarAlerta("Operación Realizada", mensaje)

        // Aquí puedes llamar a los métodos correspondientes de los servicios y realizar la lógica necesaria.
    }

    private fun mostrarAlerta(titulo: String, mensaje: String) {
        val alert = Alert(Alert.AlertType.INFORMATION)
        alert.title = titulo
        alert.contentText = mensaje
        alert.showAndWait()
    }
}
*/
