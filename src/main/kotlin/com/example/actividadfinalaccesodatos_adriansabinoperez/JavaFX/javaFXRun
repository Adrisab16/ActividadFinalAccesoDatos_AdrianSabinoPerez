// JavaFX Run:
/*
@SpringBootApplication
class ActividadFinalAccesoDatosAdrianSabinoPerezApplication: Application(){

    override fun start(primaryStage: Stage) {
        val root: Parent = FXMLLoader.load(javaClass.getResource("/fxml/MainScene.fxml"))
        val scene = Scene(root)

        primaryStage.title = "Gestión de Tiendas de Videojuegos"
        primaryStage.scene = scene
        primaryStage.show()
    }

    companion object {
        @JvmStatic
        fun main(args: Array<String>) {
            launch(MainApp::class.java)
        }
    }
}
*/